Append-only semantic topology for durable human and multi-agent work

Usage: mnema.exe [OPTIONS] <COMMAND>

Commands:
  orient      Session-start orientation: menu of active strands with catch-up commands
  add         Create a new strand with first log entry from stdin
  append      Append stdin content to a strand, or create a new strand from stdin
  show        Show full details of one strand
  list        List all strands (reverse chronological, most recent last)
  init        Initialize .mnema/ directory and journal
  checkpoint  Record context before an irreversible or state-closing action
  search      Full-text search across all log content (entry-level hits with hashes)
  find        Resolve a prefix to full strand ID
  pick        Pick a strand, then run a read/manage command on it
  link        Create a directed link between two strands
  unlink      Remove a directed link between two strands (writes an unlink effect entry)
  hide        Hide a strand from default list view
  unhide      Unhide a previously hidden strand
  close       Close a strand (write a close effect entry)
  reopen      Reopen a closed strand (write a reopen effect entry)
  explain     Explain a diagnostic code or encyclopaedia topic
  doctor      Diagnose journal integrity
  export      Export journal to a standalone audit artifact
  cutover-v2  Rewrite/import the current journal into pure v2 form
  cutover-v3  Migrate pure v2 journal into activated v3 form
  timeline    Show entry events in journal append order (timeline projection)
  tree        Build nested tree projection from strand edges
  depends     Inspect depends-on upstreams as review context
  help        Print this message or the help of the given subcommand(s)

Options:
  -C, --chdir <DIR>  Operate as if started in DIR (journal walk-up and relative paths use DIR)
  -h, --help         Print help
  -V, --version      Print version

Start here:
  Journal-wide session entry:
    mnema orient
  Delegated worker entry (local subtree):
    mnema orient --id <ID>
  Create a strand (entry text always uses stdin):
    echo "[task] ..." | mnema add

Discovery:
  Command syntax, rules, and examples:
    mnema <command> --help
  Cross-command argument contract:
    mnema explain grammar
  Concepts and diagnostic recovery:
    mnema explain <topic|CODE>

Journal discovery walks upward from the current directory, like Git.
Use -C <DIR> to choose a different starting directory. Machine consumers
should request --format json where offered; JSON fields are a public contract.
