# v2 compatibility fixture v1

Immutable historical input for the v2→v3 compatibility gate. It contains two
strands, one belongs-to edge, an entry ref, and a closed lifecycle. Never edit
files in this directory after release; add a new version instead.
